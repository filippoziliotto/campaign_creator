# 1
In entry page the subtitles for each entry are truncated in ne line
e.g. One-shot --> " a high impact missions ..."
We should have this in 2/3 lines such that is not truncated. (DONE)

# 2
After selecting the campaing type and doing stuff and turning back to the entry page
there is a "strange" right arrow on the selected campaing type
Inside the entry on the bottom part of the image
We should get rid of this. 
Maybe is the arrow next to open-forge, so we can leave it but we have to make sure that is alwyas visibile both open-forge and the arrow if they are present.


# 3
app/lib/src/ui/pages/parchment
Fix this huge files and maybe make multiple files to reduce the lenght.
Apply this fix to all files that are above 1000 lines if possible.

# 4
Change all README.md file names into the name of the folder in which is contained
e.g. folder_x/[FOLDER_X.md] (DONE)

# 5 
In the twist selection, basically all the twists are not readable till the end
we need to find a solution for this. (DONE possibily)

# 6
The badges looks good like  circles but we can improve the SVG and shape.

# 7
Add some possibilities for twists and other stuff with a "crown" symbol next to it that 
can be sbloccata by going with the "no ad version" (in that case all crowns are remoed) or by loading an ad and in that case the crown is removed once? (maybe we can improve this idea??).

# 8 
Put the Trama section as an "advanced settings". Leave it as it is but we can forge the parchment without specifying this stuff, this is already available but we can also specify
this with text saying to the user that this trama adds customiation but is not mandatory.